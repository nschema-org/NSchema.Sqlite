{
  statements: [
    CREATE VIEW "main"."active_users" AS SELECT id, email FROM main.users WHERE active,
    DROP VIEW "main"."active_users",
    CREATE VIEW "main"."active_users" AS SELECT id, email, created_at FROM main.users WHERE active,
    DROP VIEW "main"."active_users"
  ]
}